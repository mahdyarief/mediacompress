/** @type {import('next').NextConfig} */
const nextConfig = {
  // Optimize for Vercel deployment
  // Vercel automatically handles static file serving and optimization
};

export default nextConfig;
